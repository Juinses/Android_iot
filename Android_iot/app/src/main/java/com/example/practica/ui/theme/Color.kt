package com.example.practica.ui.theme

import androidx.compose.ui.graphics.Color

// --- Paleta Morada Moderna ---

// Modo CLARO
val PrimaryPurple = Color(0xFF6200EE)       // Morado intenso principal
val SecondaryTeal = Color(0xFF018786)       // Turquesa oscuro para contraste
val TertiaryPink = Color(0xFFC51162)        // Rosa fuerte para detalles/alertas

val BackgroundLight = Color(0xFFFDFBFF)     // Blanco con un tinte lavanda casi imperceptible
val SurfaceLight = Color(0xFFFFFFFF)        // Blanco puro para tarjetas

// Modo OSCURO (Colores más suaves/pastel)
val PrimaryPurpleDark = Color(0xFFBB86FC)   // Lavanda brillante
val SecondaryTealDark = Color(0xFF03DAC6)   // Turquesa brillante
val TertiaryPinkDark = Color(0xFFFF80AB)    // Rosa pastel

val BackgroundDark = Color(0xFF121212)      // Fondo oscuro estándar
val SurfaceDark = Color(0xFF1E1E1E)         // Tarjetas oscuras

// Colores base (Blanco/Negro)
val White = Color.White
val Black = Color.Black