package com.example.practica.ui.theme

import androidx.compose.ui.graphics.Color

// --- Paleta Morada Moderna (Brand Colors) ---
// Modo CLARO
val PrimaryPurple = Color(0xFF6200EE)       // Morado intenso principal
val SecondaryTeal = Color(0xFF018786)       // Turquesa oscuro para contraste
val TertiaryPink = Color(0xFFC51162)        // Rosa fuerte para detalles

val BackgroundLight = Color(0xFFFDFBFF)     
val SurfaceLight = Color(0xFFFFFFFF)        

// Modo OSCURO
val PrimaryPurpleDark = Color(0xFFBB86FC)   
val SecondaryTealDark = Color(0xFF03DAC6)   
val TertiaryPinkDark = Color(0xFFFF80AB)    

val BackgroundDark = Color(0xFF121212)      
val SurfaceDark = Color(0xFF1E1E1E)         

// --- Colores Semánticos (Estados) ---
// Usar estos en toda la app para consistencia
val SuccessGreen = Color(0xFF2E7D32)        // Éxito, Acceso Permitido, Estado Activo
val ErrorRed = Color(0xFFC62828)            // Error, Acceso Denegado, Cerrar, Bloqueado
val WarningOrange = Color(0xFFEF6C00)       // Advertencia
val InfoBlue = Color(0xFF0277BD)            // Información

// Colores base
val White = Color.White
val Black = Color.Black